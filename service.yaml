apiVersion: v1
kind: Service
metadata:
  name: backend-aji-svc
  namespace: flexcub-testing
status:
  loadBalancer: {}
spec:
  ports:
    - name: backend-aji-svc
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: backend-aji

  type: ClusterIP
  sessionAffinity: None
  ipFamilies:
    - IPv4
  ipFamilyPolicy: SingleStack
  internalTrafficPolicy: Cluster
